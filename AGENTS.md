# 工作台协作说明

先读同目录的 `agent.md`，把它视为用户持续维护的工作流说明。用户最新要求优先。

- 页面为纯 HTML、CSS、JavaScript；保持直接打开可用，不新增外部依赖或遥测。
- 保持事项字段和四区块可扩展，新增工作流时不要覆盖已有本地事项。
- 上传公开 GitHub 仓库前，检查只包含代码与通用文档。`local-data.js`、原始表格、个人备份和真实 KOL 台账均只留本机。
- 不推断没有证据的联系状态、回复日期、样机签收或脚本任务；拒绝联系者绝不进入待办。
- 完成修改后检查页面可用性，并更新 `README.md` 与 `agent.md` 中受影响的约定。
